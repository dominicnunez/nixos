# Ignore the nixos-config folder (using external repo)
nixos-config/

# Ignore build results
result
result-*

# Hardware configuration is now tracked for flake compatibility
# Each branch (main, amd, nvidia) has its own hardware-configuration.nix

# Ignore swap files
*.swp
*~
.*.swp

# Ignore lock files from editors
.#*
.claude


#Ignore vscode AI rules
.github/instructions/codacy.instructions.md
